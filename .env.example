# Runtime
FINANCEIROS_ENV=paper
FINANCEIROS_CONFIG=config/default.yaml

# Binance — só necessário para testnet/live (paper usa API pública)
# Keys de TESTNET ≠ keys de produção. Gere em https://testnet.binance.vision
# Em produção: permissão Spot Trade, SEM withdraw, com IP whitelist.
BINANCE_API_KEY=
BINANCE_API_SECRET=
